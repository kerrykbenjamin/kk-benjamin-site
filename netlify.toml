# Netlify config — the site is Netlify-ready but NOT deployed in this pass.
# Netlify auto-installs the Next.js runtime; this pins the build for clarity.
[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
